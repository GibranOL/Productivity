// DEPRECATED: Google Calendar sync will be reimplemented via Appwrite OAuth in Sprint 2.
// This is a stub to prevent build errors after removing @react-oauth/google.

export default function CalendarSync() {
  return null
}
